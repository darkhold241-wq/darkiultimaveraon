package com.darki.os

import android.app.Application

/**
 * Punto de entrada de la app. Por ahora no hace nada especial; cuando
 * lleguemos al bloque de Memoria aqui se inicializara la base de datos
 * Room que va a recordar preferencias, gustos y contexto.
 */
class DarkiApplication : Application()
